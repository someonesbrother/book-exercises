# Create a vector of 100 employees ("Employee 1", "Employee 2", ... "Employee 100")
# Hint: use the `paste()` function and vector recycling to add a number to the word
# "Employee"
employees <- c(paste("Employee", 1:100))
# Create a vector of 100 random salaries for the year 2017
# Use the `runif()` function to pick random numbers between 40000 and 50000
salaries <- c(1:100, runif(40000, 50000))
print(employees)
# Create a vector of 100 random salaries for the year 2017
# Use the `runif()` function to pick random numbers between 40000 and 50000
salaries <- c(runif(100, 40000, 50000))
print(salaries)
# Create a vector of 100 annual salary adjustments between -5000 and 10000.
# (A negative number represents a salary decrease due to corporate greed)
# Again use the `runif()` function to pick 100 random numbers in that range.
paycheck_cut <- c(runif(100, -5000, 10000))
salaries <- c(employees + yearly_pay + paycheck_cut, stringsAsFactors = FALSE)
# Create a vector of 100 random salaries for the year 2017
# Use the `runif()` function to pick random numbers between 40000 and 50000
yearly_pay <- c(runif(100, 40000, 50000))
# Create a vector of 100 employees ("Employee 1", "Employee 2", ... "Employee 100")
# Hint: use the `paste()` function and vector recycling to add a number to the word
# "Employee"
employees <- c(paste("Employee", 1:100))
# Create a vector of 100 random salaries for the year 2017
# Use the `runif()` function to pick random numbers between 40000 and 50000
yearly_pay <- c(runif(100, 40000, 50000))
# Create a vector of 100 annual salary adjustments between -5000 and 10000.
# (A negative number represents a salary decrease due to corporate greed)
# Again use the `runif()` function to pick 100 random numbers in that range.
paycheck_cut <- c(runif(100, -5000, 10000))
salaries <- c(employees + yearly_pay + paycheck_cut, stringsAsFactors = FALSE)
salaries <- c(employees + yearly_pay + paycheck_cut)
# Create a vector of 100 random salaries for the year 2017
# Use the `runif()` function to pick random numbers between 40000 and 50000
yearly_pay <- c(runif(100, 40000, 50000))
salaries <- c(paste(employees, yearly_pay + paycheck_cut), stringsAsFactors = FALSE)
print(salaries)
install.packages("stringi")
salaries <- data.frame(employees, yearly_pay + paycheck_cut, stringsAsFactors = FALSE)
print(salaries)
# Add a column to the `salaries` data frame that represents each person's
# salary in 2018 (e.g., with the salary adjustment added in).
salaries[["updated_sal"]] <- yearly_pay - paycheck_cut
# Add a column to the `salaries` data frame that represents each person's
# salary in 2018 (e.g., with the salary adjustment added in).
salaries <- salaries[["updated_sal"]] <- yearly_pay - paycheck_cut
# Add a column to the `salaries` data frame that represents each person's
# salary in 2018 (e.g., with the salary adjustment added in).
salaries[["updated_sal"]] <- yearly_pay - paycheck_cut
salaries <- data.frame(employees, yearly_pay, paycheck_cut, stringsAsFactors = FALSE)
# Add a column to the `salaries` data frame that represents each person's
# salary in 2018 (e.g., with the salary adjustment added in).
salaries[["updated_sal"]] <- yearly_pay - paycheck_cut
# Add a column to the `salaries` data frame that has a value of `TRUE` if the
# person got a raise (their salary went up)
salaries[["raise_or_nah"]] <- salaries$yearly_pay > salaries$paycheck_cut
# What was the 2018 salary of Employee 57
salaries[salaries$employees == "Employee 57", "raise_or_nah"]
nrow(salaries[salaries$raise_or_nah] == T)
nrow(salaries[salaries$raise_or_nah == T])
nrow(salaries[salaries$raise_or_nah == T])
nrow(salaries[salaries$raise_or_nah == TRUE,])
nrow(salaries[salaries$raise_or_nah == TRUE,])
nrow(salaries[salaries$raise_or_nah == TRUE])
nrow(salaries[salaries$raise_or_nah == TRUE,])
# Add a column to the `salaries` data frame that has a value of `TRUE` if the
# person got a raise (their salary went up)
salaries[["raise_or_nah"]] <- salaries$yearly_pay > salaries$updated_sal
# What was the 2018 salary of Employee 57
salaries[salaries$employees == "Employee 57", "raise_or_nah"]
nrow(salaries[salaries$raise_or_nah == TRUE,])
# What was the dollar value of the highest raise?
biggest_Raise <- max(salaries$updated_sal)
# What was the dollar value of the highest raise?
salaries[salaries$updated_sal == max(salaries$updated_sal)]
# What was the dollar value of the highest raise?
salaries[salaries$updated_sal == max(salaries$updated_sal),]
# Add a column to the `salaries` data frame that represents each person's
# salary in 2018 (e.g., with the salary adjustment added in).
salaries[["updated_sal"]] <- yearly_pay + paycheck_cut
# Exercise 2: working with data frames
# Create a vector of 100 employees ("Employee 1", "Employee 2", ... "Employee 100")
# Hint: use the `paste()` function and vector recycling to add a number to the word
# "Employee"
employees <- c(paste("Employee", 1:100))
# Create a vector of 100 random salaries for the year 2017
# Use the `runif()` function to pick random numbers between 40000 and 50000
yearly_pay <- c(runif(100, 40000, 50000))
# Create a vector of 100 annual salary adjustments between -5000 and 10000.
# (A negative number represents a salary decrease due to corporate greed)
# Again use the `runif()` function to pick 100 random numbers in that range.
paycheck_cut <- c(runif(100, -5000, 10000))
# Create a data frame `salaries` by combining the 3 vectors you just made
# Remember to set `stringsAsFactors=FALSE`!
salaries <- data.frame(employees, yearly_pay, paycheck_cut, stringsAsFactors = FALSE)
print(salaries)
# Add a column to the `salaries` data frame that represents each person's
# salary in 2018 (e.g., with the salary adjustment added in).
salaries[["updated_sal"]] <- yearly_pay + paycheck_cut
# Add a column to the `salaries` data frame that has a value of `TRUE` if the
# person got a raise (their salary went up)
salaries[["raise_or_nah"]] <- salaries$yearly_pay > salaries$updated_sal
### Retrieve values from your data frame to answer the following questions
### Note that you should get the value as specific as possible (e.g., a single
### cell rather than the whole row!)
# What was the 2018 salary of Employee 57
salaries[salaries$employees == "Employee 57", "raise_or_nah"]
# How many employees got a raise?
nrow(salaries[salaries$raise_or_nah == TRUE,])
# What was the dollar value of the highest raise?
salaries[salaries$updated_sal == max(salaries$updated_sal),]
# What was the "name" of the employee who received the highest raise?
# What was the largest decrease in salaries between the two years?
# What was the name of the employee who recieved largest decrease in salary?
# What was the average salary change?
# For people who did not get a raise, how much money did they lose on average?
## Consider: do the above averages match what you expected them to be based on
## how you generated the salaries?
# Write a .csv file of your salary data to your working directory
# What was the dollar value of the highest raise?
salaries[salaries$updated_sal == max(salaries$updated_sal), "updated_sal"]
# What was the dollar value of the highest raise?
salaries[salaries$updated_sal == max(salaries$updated_sal), "paycheck_cut"]
# What was the "name" of the employee who received the highest raise?
salaries[salaries$updated_sal == max(salaries$updated_sal), "employees"]
# What was the largest decrease in salaries between the two years?
salaries[salaries$updated_sal == min(salaries$updated_sal), "paycheck_cut"]
# What was the name of the employee who recieved largest decrease in salary?
salaries[salaries$updated_sal == min(salaries$updated_sal), "employees"]
# What was the average salary change?
mean(salaries$updated_sal < salaries$yearly_pay)
# For people who did not get a raise, how much money did they lose on average?
paycut <- salaries[salaries$raise_or_nah == F]
# For people who did not get a raise, how much money did they lose on average?
paycut <- salaries[salaries$raise_or_nah == F,]
mean(paycut$updated_sal)
mean(paycut$paycheck_cut)
# What was the average salary change?
mean(salaries$paycheck_cut)
mean(paycut$paycheck_cut)
# For people who did not get a raise, how much money did they lose on average?
mean(salaries[salaries$raise_or_nah == F,]$paycheck_cut)
# For people who did not get a raise, how much money did they lose on average?
mean(salaries[salaries$raise_or_nah == T,]$paycheck_cut)
# Exercise 2: working with data frames
# Create a vector of 100 employees ("Employee 1", "Employee 2", ... "Employee 100")
# Hint: use the `paste()` function and vector recycling to add a number to the word
# "Employee"
employees <- c(paste("Employee", 1:100))
# Create a vector of 100 random salaries for the year 2017
# Use the `runif()` function to pick random numbers between 40000 and 50000
yearly_pay <- c(runif(100, 40000, 50000))
# Create a vector of 100 annual salary adjustments between -5000 and 10000.
# (A negative number represents a salary decrease due to corporate greed)
# Again use the `runif()` function to pick 100 random numbers in that range.
paycheck_cut <- c(runif(100, -5000, 10000))
# Create a data frame `salaries` by combining the 3 vectors you just made
# Remember to set `stringsAsFactors=FALSE`!
salaries <- data.frame(employees, yearly_pay, paycheck_cut, stringsAsFactors = FALSE)
print(salaries)
# Add a column to the `salaries` data frame that represents each person's
# salary in 2018 (e.g., with the salary adjustment added in).
salaries[["updated_sal"]] <- yearly_pay + paycheck_cut
# Add a column to the `salaries` data frame that has a value of `TRUE` if the
# person got a raise (their salary went up)
salaries[["raise_or_nah"]] <- salaries$yearly_pay < salaries$updated_sal
### Retrieve values from your data frame to answer the following questions
### Note that you should get the value as specific as possible (e.g., a single
### cell rather than the whole row!)
# What was the 2018 salary of Employee 57
salaries[salaries$employees == "Employee 57", "raise_or_nah"]
# How many employees got a raise?
nrow(salaries[salaries$raise_or_nah == TRUE,])
# What was the dollar value of the highest raise?
salaries[salaries$updated_sal == max(salaries$updated_sal), "paycheck_cut"]
# What was the "name" of the employee who received the highest raise?
salaries[salaries$updated_sal == max(salaries$updated_sal), "employees"]
# What was the largest decrease in salaries between the two years?
salaries[salaries$updated_sal == min(salaries$updated_sal), "paycheck_cut"]
# What was the name of the employee who recieved largest decrease in salary?
salaries[salaries$updated_sal == min(salaries$updated_sal), "employees"]
# What was the average salary change?
mean(salaries$paycheck_cut)
# For people who did not get a raise, how much money did they lose on average?
mean(salaries[salaries$raise_or_nah == T,]$paycheck_cut)
## Consider: do the above averages match what you expected them to be based on
## how you generated the salaries?
# Write a .csv file of your salary data to your working directory
# For people who did not get a raise, how much money did they lose on average?
mean(salaries[salaries$raise_or_nah == F,]$paycheck_cut)
